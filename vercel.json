{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next"
      }
    ],
    "buildCommand": "npx prisma generate && npx prisma migrate deploy && next build",
    "installCommand": "pnpm install"
}