{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next"
      }
    ],
    "build": {
      "commands": {
        "build": "npx prisma generate && npx prisma migrate deploy && next build"
      }
    }
  }
  